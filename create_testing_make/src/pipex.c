#include "../include/pipex.h"

/* int main(void)
{
	int *leak = malloc(10 * sizeof(int));
	printf("%p", leak);
	printf("iniciando o pipex\n");
	print_hello();
	print_error("error de test");

	return (0);
} */
